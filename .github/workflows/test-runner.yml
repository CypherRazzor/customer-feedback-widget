name: Test runner (DIG-2660 debug)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Hello
        run: echo "Runner works - permissions OK"
      - name: Check Docker
        run: docker version
      - name: Check arch
        run: uname -m
